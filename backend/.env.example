PORT=3000
MONGO_URI=mongodb://mongo:27017/webhook_engine
REDIS_HOST=redis
REDIS_PORT=6379
# Per-tenant webhook signing secrets are stored on the Tenant document itself,
# this is only a fallback used by the seed script.
DEFAULT_WEBHOOK_SECRET=dev-secret-change-me
